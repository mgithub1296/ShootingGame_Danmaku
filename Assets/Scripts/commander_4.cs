using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;

public class commander_4 : MonoBehaviour
{/*
    //動かす敵を指定
    public GameObject[] enemys;
    // Enemy_3コンポーネント
    private Enemy_4 enemy_4;
    //到達点
    public Vector3[] arrivalPoint;


    // Start is called before the first frame update
    IEnumerator Start()
    {
        // Enemy_3コンポーネントをシーン内から探して取得する
        enemy_4 = FindObjectOfType<Enemy_4>();


        //動かす敵を指定
        for (int i = 0; i < enemys.Length; i++)
        {
            //引数①動かす敵　引数②到達点　引数③到達点その2（配列の半分は引数②なので）
            enemy_4.AttackSequence(enemys[i], arrivalPoint[i], arrivalPoint[i + (arrivalPoint.Length / 2)]);


            // 0.5秒待つ
            yield return new WaitForSeconds(0.5f);
        }
    }
    */
}

